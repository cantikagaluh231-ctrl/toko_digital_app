name: toko_digital_app
description: "Aplikasi Toko Roti & Kue"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  provider: ^6.0.0
  sqflite: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Mengaktifkan folder assets untuk gambar
  assets:
    - assets/images/